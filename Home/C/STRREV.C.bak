#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
	char x[25],rev[25];
	int i,len,j;
	clrscr();
	printf("Enter Your Name : \n");
	gets(x);
	len = strlen(x);
	for(i=0 ,j=len;i<len, j>=0;i++, j--)
	{

		rev[j] = x[i];

	}
	for(j=0;j<=len;j++)
	{
		printf("%c",rev[j]);
	}
	getch();
}